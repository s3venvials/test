In order to run this project you must have the following installed and set in your system environment variable paths.

Python3,
Selenium Webdriver for Chrome,
and Node.js

Install node modules with 'npm install'

start with 'node app.js'

Navigate to localhost:3000